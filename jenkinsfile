pipeline{
    agent any
    
    stages{
        stage("deploy"){
            steps{
                echo "install simple-web"
                sh 'helm install simple-web ./simple-web'
            }
        }
    }
}